/** ISR público: regeneración periódica; el admin dispara revalidatePath al guardar. */
export const PUBLIC_PAGE_REVALIDATE = 60

/** Sitemap: actualización menos frecuente (URLs estables). */
export const SITEMAP_REVALIDATE = 3600
